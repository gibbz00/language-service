[package]
name = "language-service-json-rpc"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
lsp-types = "0.94.1"

[dev-dependencies]
once_cell = "1"
